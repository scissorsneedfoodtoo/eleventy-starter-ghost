{% macro card(post, siteLang) %}
    <article class="post-card {{post.class}}">
        {% if post.feature_image %}
            <a class="post-card-image-link" href="{{post.url}}">                
                {% 
                    featureImage
                    post.feature_image,
                    "post-card-image",
                    post.title,
                    [
                        {
                            mediaStr: "(max-width: 365px)",
                            width: 300
                        },
                        {
                            mediaStr: "(max-width: 655px)",
                            width: 600
                        },
                        {
                            mediaStr: "(max-width: 767px)",
                            width: 1000
                        },
                        {
                            mediaStr: "(min-width: 768px)",
                            width: 300
                        }
                    ]
                %}
            </a>
        {% else %}
            <div class="no-feature-image-offsetter"></div>
        {% endif %}

        <div class="post-card-content">
            <div class="post-card-content-link">
                <header class="post-card-header">
                    {%if post.primary_tag %}
                        <span class="post-card-tags">
                            <a href="{{post.primary_tag.url}}">
                                #{{post.primary_tag.name}}
                            </a>
                        </span>
                    {% endif %}
                    <h2 class="post-card-title">
                        <a href="{{post.url}}">
                            {{post.title}}
                        </a>
                    </h2>
                </header>
            </div>

            <footer class="post-card-meta">
                {% if post.primary_author.name == "freeCodeCamp.org" %}
                    <time class="meta-item-single" datetime="{{ post.published_at }}">{% timeAgo post.published_at, siteLang %}</time>
                {% else %}
                    <ul class="author-list">
                        {% for author in post.authors %}
                            <li class="author-list-item">
                                <div class="author-name-tooltip">
                                    {{ author.name }}
                                </div>
                                {% if author.profile_image %}
                                    <a href="{{ author.url}}" class="static-avatar">
                                        {% 
                                            image
                                            author.profile_image,
                                            "author-profile-image",
                                            author.name,
                                            "30px",
                                            [30]
                                        %}
                                    </a>
                                {% else %}
                                    <a href="{{author.url}}" class="static-avatar author-profile-image">Add avatar icon</a>
                                {% endif %}
                            </li>
                        {% endfor %}
                    </ul>
                    <span class="meta-content">
                        <a class="meta-item" href="{{post.primary_author.url}}">{{post.primary_author.name}}</a>
                        <time class="meta-item" datetime="{{ post.published_at }}">{% timeAgo post.published_at, siteLang %}</time>
                    </span>
                {% endif %}
            </footer>
        </div>
    </article>
{% endmacro %}
