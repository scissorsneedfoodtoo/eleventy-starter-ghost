<section class="author-card">
  {% if post.primary_author.profile_image %}
        {% 
            image
            post.primary_author.profile_image,
            "author-profile-image",
            author.name,
            "60px",
            [60]
        %}
    {% else %}
        <span class="avatar-wrapper">{% include "partials/icons/avatar.njk" %}</span>
    {% endif %}
    <section class="author-card-content">
        <h4 class="author-card-name">
            <a href="{{ post.primary_author.url }}">{{ post.primary_author.name }}</a>
        </h4>
        {% if post.primary_author.bio %}
            <p>{{ post.primary_author.bio }}</p>
        {% else %}
            <p>Read <a href="{{ post.primary_author.url }}">more posts</a> by this author.</p>
        {% endif %}
    </section>
</section>
